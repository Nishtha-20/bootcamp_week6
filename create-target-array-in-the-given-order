class Solution(object):
    def createTargetArray(self, nums, index):
        """
        :type nums: List[int]
        :type index: List[int]
        :rtype: List[int]
        """
        l, ans=len(nums),[]
        for i in range(l):
            ans.insert(index[i],nums[i])
        return ans
        
